
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.6.1
  creationTimestamp: null
  name: redisbackups.redis.middleware.alauda.io
spec:
  group: redis.middleware.alauda.io
  names:
    kind: RedisBackup
    listKind: RedisBackupList
    plural: redisbackups
    singular: redisbackup
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: RedisBackup is the Schema for the redisbackups API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: RedisBackupSpec defines the desired state of RedisBackup
            properties:
              image:
                type: string
              source:
                properties:
                  redisFailoverName:
                    type: string
                  storageClassName:
                    type: string
                required:
                - redisFailoverName
                - storageClassName
                type: object
              storage:
                anyOf:
                - type: integer
                - type: string
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
            type: object
          status:
            description: RedisBackupStatus defines the observed state of RedisBackup
            properties:
              completionTime:
                format: date-time
                type: string
              condition:
                type: string
              destination:
                description: where store backup data in
                type: string
              jobName:
                type: string
              lastCheckTime:
                format: date-time
                type: string
              message:
                description: show message when backup fail
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
